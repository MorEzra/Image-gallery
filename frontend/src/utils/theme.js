import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    purpleLight: "#8D9EFF",
    purple: "#667AFF",
    gray: "#808080",
    grayDark: "#282c34",
    grayMid: "#E4E2EA",
    grayLight: "#F9F9FB",
    white: "#ffffff",
  },
});
